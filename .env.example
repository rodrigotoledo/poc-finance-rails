# Copie para .env e preencha os valores
# NUNCA commite o .env — apenas o .env.example

# Rails
RAILS_MASTER_KEY=...
RAILS_ENV=...

# Postgres
DATABASE_URL=postgresql://postgres:postgres@db:5432/credito_poc_production

# Background jobs (Solid Queue → Postgres, não Redis)
SOLID_QUEUE_DB=credito_poc_queue

# Segurança — gerado automaticamente pelo make setup
SECRET_KEY_BASE=...
BCRYPT_COST=12

# BACEN / Compliance (para mocks locais)
BACEN_API_URL=https://api.bcb.gov.br
BACEN_API_KEY=mock_bacen_key_dev
FORCE_DB_CREATE=false
FORCE_DB_SEED=false